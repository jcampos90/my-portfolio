# Copy to .env for local development. None of these are secrets.

# Cloudflare Web Analytics beacon token. Public site identifier, not a secret.
# Dashboard → Analytics & Logs → Web Analytics → your site → JS snippet.
# Leave empty to render no analytics at all.
NEXT_PUBLIC_CF_ANALYTICS_TOKEN=
